#100D - Final UI Correction + Runtime Tab Relocation + V3-Only Documentation Purge and Rewrite

## What & Why
Task #100C.2 cleaned up the UI substantially, but there are still 3 unacceptable issues left:

1. **Export is still wrong**
   - Export still lives in Research
   - Export is still using the old CSV/JSON export routine
   - That old export path is obsolete, broken, and must be deleted
   - We already built the correct ZIP export pipeline with canonical candle data, manifest.json, validation.json, and chunk files
   - The UI must use that ZIP export routine only

2. **Runtime content was lost**
   - The useful Runtime tab content from Diagnostics was not relocated properly
   - We were previously vague about whether it should go to Data or Decisions
   - This task is explicit: **move the useful Runtime content into the Data page**
   - Diagnostics should remain advanced/debug-only and not own runtime operational content

3. **Docs are still dangerous**
   - Old docs, old ideas, old V2 assumptions, old scoring assumptions, old architecture language, old fallback concepts, and stale references must be removed completely
   - Do NOT archive them
   - Do NOT keep them in legacy-docs
   - Do NOT leave them around “for reference”
   - DELETE old docs and replace them with clean V3-only docs
   - The AI/replit agent must not see mixed V2/V3 guidance anymore

This is the final cleanup task before exporting clean data for external re-analysis.

---

## Absolute rules

### 1. Delete the old export UI and old export path references
The CSV/JSON export UI and any references to it in the primary app must be removed.
Do NOT keep it as a fallback.
Do NOT keep it hidden.
Do NOT keep it “just in case”.

### 2. Use the correct ZIP export routine only
The UI must point only to the current canonical ZIP export system:
- symbol
- timeframe
- valid date range
- precheck
- selected-range stats
- canonical ZIP export
- validation.json
- manifest.json
- chunked raw candle files

No other export routine should remain in primary ownership.

### 3. Runtime content must move into Data
Do not leave runtime operational content floating in Diagnostics.
Move the useful Runtime content into Data explicitly.

### 4. Delete old docs completely
Under no circumstances keep old docs around in the repo in any primary or secondary readable location.
If a document is outdated, mixed, V2-based, unclear, or refers to retired architecture:
- DELETE it
- REWRITE it as V3-only if still needed

No “archive”
No “legacy docs”
No “deprecated docs”
No fallback docs

### 5. No fallback logic, no commented-out code, no soft deprecation
Delete replaced code and docs.
Do not comment out old content.
Do not leave unused files behind.
Fail loudly if a new path is missing.

### 6. This is still not a scoring/engine rewrite task
Do NOT redesign scoring or allocator logic here.
Only clean UI ownership and documentation alignment.

---

## Done looks like
- Export no longer appears in Research
- Export lives in Data with the correct ZIP export flow only
- old CSV/JSON export UI is gone
- Runtime operational content now lives in Data
- Diagnostics is reduced to advanced/debug-only content
- all docs in the repo are rewritten or deleted so that only V3 truth remains
- no V2 documentation, stale architecture notes, or mixed old guidance remain anywhere the AI can read

---

## Out of scope
- no engine scoring redesign
- no allocator redesign
- no backend strategy rewrite
- no re-architecture of the live trading system
- no cosmetic redesign unrelated to these ownership fixes

---

## Part 1 - Export ownership correction

### 1. Remove Export from Research
Delete the Export tab/section from Research entirely.

Research should contain only:
- AI Analysis
- strategy/research output
- any real research-only views still valid

Research must not own export anymore.

### 2. Add Export to Data
Move Export into Data and make it a proper Data tab/section.

Required owner:
- Data page only

### 3. Delete old export UI completely
Delete the current CSV/JSON export UI and any code/components/routes/helpers wired to that obsolete export flow.

This includes:
- old format selectors (CSV / JSON)
- old download buttons tied to the broken legacy export
- old export endpoint usage in frontend
- any obsolete primary export component

Do not keep any of it.

### 4. Wire Data Export to the correct ZIP export routine only
The Data page export section must use the correct current export pipeline:
- symbol selector
- timeframe selector
- actual available date range
- precheck
- selected-range stats
- ZIP download only

It must display the proper export truth:
- available range
- selected range
- selected-range row count
- real candle count
- interpolated candle count
- first date
- last date
- total available dataset summary if useful

It must trigger the canonical ZIP export only.

### 5. Export UX requirements
The new Export section in Data must:
- use the current real available date bounds
- clamp/reject invalid dates
- show loading / success / failure clearly
- not expose obsolete format choices
- explain what the ZIP contains:
  - raw candle chunks
  - manifest.json
  - validation.json

---

## Part 2 - Runtime relocation into Data

### 6. Move Runtime operational content into Data
The useful Runtime content that previously existed under Diagnostics must now live in Data.

Specifically move the operational runtime content into Data, such as:
- System Overview runtime summary if still useful there
- V3 Engine Features / Live State
- active scanner/runtime state that matters operationally

Do NOT leave this content stranded in Diagnostics if it belongs to operational use.

### 7. Decide exact placement inside Data
Place runtime content in Data in a clean way.

Suggested structure inside Data:
- Symbol State
- Coverage
- Data Operations
- Top-Up
- Live View
- Export
- Runtime / Engine Live State

You may merge Runtime into a sensible existing tab if cleaner, but it must be clearly available in Data.

### 8. Diagnostics must remain advanced/debug-only
After moving Runtime out, Diagnostics should contain only true debug/advanced material.
It must not remain the owner of normal operational content.

---

## Part 3 - Hard documentation purge and rewrite

### 9. Audit all repo documentation and instruction surfaces
Search the repo thoroughly for:
- V2 references
- legacy scoring references
- outdated architecture references
- old route/page descriptions
- fallback language
- deprecated system descriptions
- stale help text
- mixed V2/V3 instructions
- old task summaries that still describe retired behavior
- old skill guidance
- brain/context files with obsolete assumptions

Include at minimum:
- SKILL.md / skill docs
- README / project docs
- help/about content
- architecture docs
- brain/context files
- any repo-local agent instructions
- any UI text/config docs
- any task or operation docs that still shape AI behavior

### 10. Delete old docs, do not archive
If a doc is old, mixed, stale, V2-oriented, or unclear:
- DELETE it

If a doc is still needed:
- REWRITE it fully as V3-only

Do not keep old versions under any other folder.
Do not archive them.
Do not leave “for reference” copies.

### 11. Rewrite the essential V3 docs from scratch
Create/replace only the docs that are actually needed, written clearly for V3 only.

At minimum ensure the surviving docs define:
- V3 architecture
- active trading symbols
- 28 connected/available symbols for research/data
- 4 active streaming trading symbols by default
- candles table as canonical truth
- ticks as live/support data
- interpolation as last resort only
- export is ZIP-only and raw
- no fallback logic
- no legacy V2 scoring assumptions as current truth
- current UI ownership by page
- Diagnostics is advanced/debug-only
- fail loudly principle

### 12. Rewrite UI-facing help text and descriptions
Update any app help/about/descriptions/tooltips/subtitles so they align with V3 only.
Remove old language such as:
- old family model language
- stale scoring assumptions presented as settled truth if they are not
- legacy export wording
- legacy diagnostics wording

### 13. Remove stale settings/help language
If Settings or Help still describe old V2 concepts, rewrite or remove them.
Do not leave mixed language for AI or users to interpret.

---

## Part 4 - Hard cleanup and verification

### 14. Delete dead code after ownership moves
After moving Export and Runtime and rewriting docs:
- remove dead imports
- remove obsolete components
- remove unused routes
- remove unused helpers tied only to the deleted old export UI
- remove stale diagnostics-only ownership code if no longer used

No commented-out code.
No dead files left behind.

### 15. Verify the final primary ownership
After this task:
- Data owns export and runtime operational content
- Research owns AI analysis only
- Diagnostics is advanced/debug-only
- old export UI is gone
- docs are V3-only

---

## Required deliverables
Return:

1. exact frontend files/modules changed
2. exact frontend files/modules removed
3. exact frontend files/modules added
4. exact tiny backend files changed for UI truth only
5. proof Export was removed from Research
6. proof Export now lives in Data
7. proof old CSV/JSON export UI was deleted
8. proof the correct ZIP export routine is now the only export path in the UI
9. proof Runtime content moved into Data
10. what remains in Diagnostics and why
11. exact doc files deleted
12. exact doc files rewritten
13. exact doc files newly created if any
14. proof no old V2/mixed docs remain in readable repo locations
15. build/test result
16. explicit confirmation:
   - old export UI is gone
   - Data now owns export
   - Data now owns runtime operational content
   - Diagnostics is no longer a primary owner of operational content
   - old docs were deleted, not archived
   - surviving docs are V3-only aligned

---

## Verification requirements
You must verify with real runs:
- Research no longer shows export
- Data shows the correct ZIP export flow
- export precheck works
- ZIP export download path is the canonical one
- runtime content is visible in Data
- Diagnostics still loads but only as advanced/debug
- docs search shows no old V2/mixed primary docs remaining
- build passes
- app loads cleanly

Do not fake verification.

---

## Quality bar
This task succeeds only if:
- the UI is finally clean enough to use for exporting clean symbol data
- the old export path is gone
- runtime content is in the correct place
- the repo’s documentation/instruction layer is fully V3-aligned
- no stale V2 doc guidance remains to contaminate future AI work

If any old export UI or old mixed docs remain, the task has failed.
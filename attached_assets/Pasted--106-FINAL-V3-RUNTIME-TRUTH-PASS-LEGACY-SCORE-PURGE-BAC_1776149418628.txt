#106 — FINAL V3 RUNTIME TRUTH PASS, LEGACY SCORE PURGE, BACKTEST JSON EXPORT, AND RAILWAY CLEAN REBUILD

## What & Why
The system is now mostly V3, but it is NOT yet clean or trustworthy enough for paper-mode history building and backtest review.

Current live issues confirmed:
1. Railway boots, but runtime is broken:
   - scheduler starts at 300s and is then overridden back to 60s
   - stagger scan repeatedly fails against candles
   - DB pool eventually terminates unexpectedly
2. The system still has stale 85/90/92 logic and old shared-score references persisting in docs / skills / platform state / visibility logic / possibly UI copy
3. Backtest works, but JSON export is still missing
4. Railway still needs a deterministic rebuild/reset path that leaves only the true V3 state behind

This task is the final cleanup and runtime-truth pass before deeper backtest review and further tuning.

---

## NON-NEGOTIABLE RULES

### 1. DELETE all 85 / 90 / 92 legacy score-threshold logic wherever it is no longer current
If 85/90/92 still exists in:
- docs
- skills
- repo instructions
- defaults
- platform_state seeding
- UI labels
- visibility thresholds
- comments
- helper files
- archived-but-still-active code
then remove or rewrite it.

Do NOT preserve that language anywhere in active V3 guidance.

### 2. DO NOT change engine-native scoring formulas in this task
This is not a score-model redesign task.
Do not touch:
- engine native component formulas
- engine native weights
- engine gates
- backtest logic
unless required for runtime correctness

### 3. FIX runtime truth first
Do not claim the system is ready while scheduler/scans are still failing in production.

### 4. FAIL LOUD
If schema is incomplete, startup must not proceed into scanning and pretending V3 is healthy.

### 5. No V2 fallback, no comment-out mess
Delete or rewrite.
Do not hide old logic.

---

# PART 1 — FIX LIVE RUNTIME / RAILWAY TRUTH

## T001 — Find why scan interval is reverting to 60s
Live log shows:
- baseline scan starts at 300s
- then scheduler updates scan interval back to 60s

Find the exact source of this override.
It may be:
- stale platform_state seed
- stale runtime update path
- old settings load
- compatibility shim
- hardcoded fallback

### Required fix
Ensure the system uses the correct intended V3 scan cadence and does not silently revert to 60s unless explicitly and currently intended.

If 60s is no longer desired as the baseline, delete the path causing the override.

---

## T002 — Fix candles schema/runtime truth
Live log shows repeated scan failures querying candles columns:
- `source`
- `is_interpolated`

The app is clearly expecting those columns.
The runtime/schema path is still not truthfully aligned.

### Required fix
Audit:
- current candles schema
- startup migrations
- “setup already complete” logic
- schema-ready marker logic
- any guarded migration path

Then make startup truthfully verify:
- required tables exist
- required columns exist
- required indexes exist if relied on
before scanning begins

If columns are missing:
- apply idempotent migration
- OR fail loudly before scheduler starts

Do not allow the app to say:
- setup complete
- schema ready
while scans are guaranteed to fail.

---

## T003 — Stop scheduler from scanning if DB/schema is not valid
Currently the app boots and scheduler starts, even though scans immediately fail.

### Required fix
Startup order must become:
1. DB connect
2. schema verification / migration
3. runtime state verification
4. ONLY THEN start scheduler + streaming

If schema/runtime truth fails:
- no stagger scan
- no fake healthy startup

---

## T004 — Stabilize DB runtime under current load
Live log also shows:
- DB pool error
- connection terminated unexpectedly
- failed tick insert

Audit whether this is being triggered by:
- failing scans hammering the DB
- too many concurrent operations
- bad retry logic after schema failure
- duplicate reconnect loops

### Required fix
Once schema/runtime truth is fixed, ensure the DB layer no longer collapses under the same startup/runtime path.

Do not over-engineer.
Fix the actual failure mode.

---

## T005 — Validate Railway runtime cleanly
You must validate all of this against the actual Railway-compatible build path.

Success means:
- app boots
- schema verifies truthfully
- scheduler starts only after DB ready
- no repeated stagger scan candle errors
- no immediate DB pool collapse
- no broken runtime pretending to be healthy

---

# PART 2 — PURGE 85 / 90 / 92 AND OLD SHARED-SCORE LANGUAGE COMPLETELY

## T006 — Search the entire repo for 85 / 90 / 92 threshold references
Search and audit for:
- `85`
- `90`
- `92`
when they refer to old mode thresholds
and all old shared-score language tied to them

Also search for:
- old 5-dimension score descriptions
- composite-score language from older systems
- “big move readiness” old scoring docs
- old signal visibility defaults
- stale V2 or V2.5 threshold guidance

### Required fix
Delete or rewrite every stale active reference.

If any threshold remains active in current V3, document why.
Otherwise remove it.

---

## T007 — Rewrite Help to current V3 only
Help must:
- identify current version as V3
- remove stale V2 language
- remove stale threshold statements
- reflect current app structure:
  - Data
  - Research (AI Analysis + Backtest)
  - Decisions
  - Trades
  - Settings
  - Help
  - Diagnostics/debug if still intentionally present
- reflect 4 active symbols and 8 live engines
- reflect that backtest exists in Research

No mixed guidance.

---

## T008 — Rewrite replit.md fully to current V3 truth
`replit.md` must become a clean source of truth.

It must not contain:
- stale 85/90/92 language
- old shared-score descriptions
- outdated architecture claims
- old route ownership
- outdated tasks as if still current

It must reflect:
- current live V3 path
- current scheduler/lifecycle truth
- current reset/rebuild truth
- current backtest availability
- current active symbols
- current engine-native scoring model
- any still-provisional items clearly labeled as provisional

---

## T009 — Rewrite all repo-governing skills/docs/artifacts/memory files
Inspect and clean:
- `.agents/skills/deriv-trading-repo-guardian/SKILL.md`
- any repo-local skill docs
- any memory/context/brain files
- any artifacts or markdown files that steer future AI work

Important:
The uploaded repo search shows at least one skill/doc still describing the old 5-dimension score system and old per-symbol breakpoints. That must be removed or explicitly marked historical/backtest-only if intentionally archived.

No active doc may teach stale logic.

---

# PART 3 — BACKTEST JSON EXPORT

## T010 — Add simple JSON export to Research → Backtest
Backtest already works in the UI.
Add a simple export that outputs:

### File 1 — summary JSON
Include at minimum:
- symbol
- engine or engine set
- date range
- total trades
- win rate
- avg return
- total return
- max drawdown
- trades per month
- monthly return
- any additional summary metrics already returned

### File 2 — trades JSON
Include full individual trade rows:
- symbol
- engine
- direction
- entryTime
- exitTime
- entryPrice
- exitPrice
- holdDurationMinutes or equivalent
- maxFavourableMove
- maxDrawdown
- exitReason
- profitPercent

### UI rules
- visible only when result exists
- no fake export
- simple JSON download is enough
- do not add ZIP unless trivial

---

# PART 4 — RAILWAY CLEAN REBUILD / RESET FLOW

## T011 — Create deterministic V3 rebuild/reset script
Create a single explicit reset/rebuild script for Railway.

Suggested location:
- `artifacts/api-server/scripts/v3-railway-rebuild.sql`
or equivalent

### Preserve only
- `ticks`
- `candles`

### Clear
- `trades`
- `signal_log`
- `backtest_runs`
- `backtest_trades`
- stale AI suggestion/cache state
- obsolete runtime state
- stale platform_state keys from prior versions
- any state causing old threshold/old scheduler values to reappear

### Re-seed only current V3-required state
No old defaults.
No V2 remnants.
No dead settings.

---

## T012 — Make rebuild/reset explicit and re-runnable
The rebuild/reset flow must:
- be safe to rerun
- document what it preserves
- document what it clears
- leave the system in a valid V3 state
- not run automatically unless explicitly enabled

Preferred:
- manual command
- or guarded env flag

---

## T013 — Rebuild/reset must include schema truth verification
After reset/rebuild:
- schema must truly match code
- “setup complete” must mean setup complete
- scheduler must not start unless schema is valid

---

# PART 5 — ENGINE DECISIONS VISIBILITY FIX

## T014 — Fix blocked/rejected signal visibility
The Engine Decisions tab must show blocked/rejected signals that are already being scanned.

Implement all of the following:

### A. execution_status fix
When a signal is rejected before execution, do NOT write lifecycle states like:
- idle
- watch
- qualified
into `signal_log.execution_status`

Write:
- `blocked`
for blocked/rejected rows

Lifecycle state remains internal.
DB execution_status must be semantic and UI-correct.

### B. visibility threshold cleanup
If `signal_visibility_threshold` is still seeded or defaulted from old values, clean it up.
The visibility threshold must no longer be tied to stale 75 / 85 / 90 / 92-era logic.

Use the intended current V3 visibility behavior.

### C. rejection_reason visibility
Ensure rows with `rejection_reason IS NOT NULL` are always surfaced in the Decisions tab, regardless of internal lifecycle state.

Do NOT redesign the Decisions UI.
Just make it show the real scanned signals correctly.

---

# PART 6 — VALIDATION

## T015 — Validate runtime
Confirm:
- no scan-interval silent reversion unless intentionally current
- no repeated candles scan errors
- no schema lie at startup
- no immediate DB pool collapse under current live path

## T016 — Validate docs/skills cleanup
Confirm:
- Help reflects current V3 only
- replit.md reflects current V3 only
- repo skills/docs reflect current V3 only
- 85/90/92 old-threshold language is gone from active repo guidance
- old shared-score guidance is gone or clearly marked historical

## T017 — Validate backtest export
Confirm:
- run backtest in Research
- export summary JSON
- export trades JSON
- files contain correct fields
- no regression in Backtest UI

## T018 — Validate rebuild/reset
Confirm:
- preserves only ticks + candles
- clears all intended operational/runtime tables/state
- reseeds only current V3 requirements
- app starts into a clean valid V3 state after rebuild/reset

## T019 — Validate Engine Decisions
Confirm:
- blocked/rejected signals now appear
- rejection reasons are visible
- allowed executions still appear
- no live execution logic changed

---

# EXPLICIT OUT OF SCOPE

Do NOT in this task:
- change engine-native scoring formulas
- redesign engine gates
- redesign backtest logic
- redesign lifecycle
- add new strategies
- do a broad UI overhaul

This task is only:
- runtime truth
- legacy threshold purge
- docs/skills cleanup
- backtest JSON export
- rebuild/reset
- decisions visibility

---

# DONE CRITERIA

This task is complete only if:
1. Railway runtime is actually healthy under live scan conditions
2. scheduler no longer silently reverts to stale scan settings
3. candles schema/runtime truth is verified before scanning
4. 85/90/92 stale threshold logic is removed from active repo guidance and defaults
5. old shared-score doc/skill content is removed or clearly marked historical
6. Help is current V3 only
7. Backtest JSON export works
8. Railway rebuild/reset exists and preserves only ticks + candles
9. Engine Decisions shows blocked/rejected signals correctly
10. TypeScript/build passes
11. Final report is returned in the exact format below

---

## REQUIRED FINAL RESPONSE FORMAT

### Task #106 complete
1. Files changed
2. Railway/runtime root causes fixed
3. Scan-interval override source and fix
4. Schema/startup truth fix
5. Help/docs/skills cleanup summary
6. 85/90/92 legacy-threshold purge summary
7. Backtest JSON export implementation
8. Railway rebuild/reset script details
9. Preserved vs cleared tables/state
10. Engine Decisions visibility fix summary
11. Validation results
12. TypeScript/build result
13. Explicit confirmations:
   - Railway runtime is now healthy under live scan conditions
   - scheduler no longer silently reverts to stale scan settings
   - startup verifies schema truth before scanning
   - 85/90/92 stale logic is gone from active V3 guidance/defaults
   - backtest JSON export works
   - rebuild/reset preserves only ticks + candles
   - Engine Decisions now surfaces blocked/rejected signals correctly
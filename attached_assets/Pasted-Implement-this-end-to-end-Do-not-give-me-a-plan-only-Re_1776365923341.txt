Implement this end to end. Do not give me a plan only. Read the repo, implement changes, then report exactly what was done and what remains.

Objective:
Upgrade the app’s AI system to use GPT-5.1 as the primary reasoning model AND implement a proper context retrieval layer using text-embedding-3-large so the in-app AI can understand:
- repo code
- database schema and data summaries
- strategies and engines
- backtest outputs
- calibration outputs

This must power:
- AI Analysis
- Move Calibration passes
- future in-app chatbot

AI remains RESEARCH ONLY. It must NOT enter the live trading loop.

--------------------------------------------------

NON-NEGOTIABLE RULES

1. GPT-5.1 is the primary model for all reasoning/research
2. text-embedding-3-large must be used for retrieval
3. AI must NOT affect live trading decisions
4. Retrieval must be modular and reusable across:
   - AI Analysis
   - Move Calibration
   - future chat assistant
5. Do NOT hardcode context into prompts — it must come from retrieval
6. Do NOT break existing system functionality

--------------------------------------------------

PART 1 — MODEL CONFIGURATION

Task 1:
Find all current OpenAI usage and replace active reasoning model with:

- Primary: gpt-5.1
- Optional fallback: gpt-5-mini

Centralize model configuration:
- one config file/module for model names
- remove scattered hardcoded model strings

--------------------------------------------------

PART 2 — BUILD CONTEXT RETRIEVAL LAYER

Task 2:
Create a retrieval system using embeddings:

Model:
- text-embedding-3-large

Create a new module:
core/ai/contextRetriever.ts

This module must:
- generate embeddings for text
- store embeddings
- retrieve top-k relevant context for a query

--------------------------------------------------

Task 3:
Define context sources to index:

1. REPO CODE
- key source files
- engine logic
- calibration modules
- strategy definitions

2. DATABASE SCHEMA
- tables
- fields
- relationships

3. DATA SUMMARIES (NOT RAW DATA)
- recent trades
- signal summaries
- move summaries
- calibration outputs

4. STRATEGY DEFINITIONS
- BOOM300 logic
- CRASH300 logic
- R_75 engines
- R_100 engines

5. CALIBRATION OUTPUTS
- detected moves
- profiles
- pass results

--------------------------------------------------

Task 4:
Create embedding storage

If existing DB is used:
- create table: ai_context_embeddings

Fields:
- id
- source_type (code | schema | strategy | calibration | data_summary)
- source_id
- content_text
- embedding_vector
- metadata_json
- created_at

--------------------------------------------------

Task 5:
Create ingestion process

Build function:
- indexRepoContext()
- indexSchemaContext()
- indexStrategyContext()
- indexCalibrationContext()

These should:
- chunk content
- generate embeddings
- store them

Do NOT embed entire files blindly
- chunk intelligently (functions, modules, sections)

--------------------------------------------------

PART 3 — RETRIEVAL + GPT-5.1 INTEGRATION

Task 6:
Modify AI calls to use retrieval

Before calling GPT-5.1:

1. take user query or pass input
2. retrieve top-k relevant context (k=5–10)
3. inject into prompt as structured context

Prompt format:

CONTEXT:
- [retrieved chunk 1]
- [retrieved chunk 2]
...

QUESTION:
[user query or task]

--------------------------------------------------

Task 7:
Apply retrieval to:

- AI Analysis
- Move Calibration passes (precursor, trigger, behavior, extraction)
- any existing chatbot route

--------------------------------------------------

PART 4 — FIX CURRENT FAILURES

Task 8:
Fix 403 errors by:
- removing gpt-4o usage
- replacing with gpt-5.1

Ensure:
- AI Analysis works
- Move Calibration passes run

--------------------------------------------------

PART 5 — MOVE CALIBRATION UI FIXES

Task 9:
Fix strategy selector

Make it symbol-aware:

- BOOM300 → only boom strategy
- CRASH300 → only crash strategy
- R_75 → reversal / continuation / breakout
- R_100 → reversal / continuation / breakout

--------------------------------------------------

Task 10:
Fix window selector

Replace:
- 14d–180d

With:
- 1 month
- 3 months
- 6 months
- 9 months
- 12 months

--------------------------------------------------

Task 11:
Fix Current Engine Behavior panel

If it depends on /api/behavior/profile:

- either:
  - add a UI button: "Build Behavior Profile"
- or:
  - auto-trigger when missing

Remove confusing raw API instruction text

--------------------------------------------------

PART 6 — CHAT ASSISTANT (FOUNDATION ONLY)

Task 12:
If a chat route/component exists:

- switch it to GPT-5.1
- connect retrieval layer

If not:
- create a simple backend route:
  POST /api/ai/chat

It must:
- accept user query
- retrieve context
- call GPT-5.1
- return response

--------------------------------------------------

PART 7 — VALIDATION

Task 13:
Confirm:

- GPT-5.1 is used everywhere for AI
- embeddings are being created and stored
- retrieval returns relevant context
- AI Analysis works
- Move Calibration runs without errors
- selectors are fixed
- UI is clearer
- build passes

--------------------------------------------------

OUTPUT FORMAT

1. Files changed
2. Model configuration summary
3. Embedding system implementation
4. Context sources indexed
5. Retrieval pipeline summary
6. GPT-5.1 integration summary
7. AI Analysis status
8. Move Calibration status
9. UI fixes summary
10. Chat assistant status
11. Validation results
12. TypeScript/build result
13. What is still missing for a full intelligent system
14. Confirm:
   - GPT-5.1 is active
   - text-embedding-3-large is used
   - retrieval is working
   - AI is not in trading loop
We are doing a controlled V1 reset-and-coherence refactor.

Read this carefully before making any changes.

## CORE INSTRUCTION

Do NOT keep layering new menus, settings, functions, or partial refactors on top of old ones.

The current problem is that the app feels disconnected and incoherent:
- old logic is lingering
- outdated menus/settings still exist
- refactors have been added on top of prior logic instead of replacing it cleanly
- deploys do not return the system to a clean first-run state

This pass is about making the app coherent and deterministic.

The system must behave like one integrated trading application, not a pile of partially replaced features.

---

# PRIMARY OBJECTIVE

Every code redeploy that triggers a rebuild in Railway must reset the app to a clean fresh first-run state.

That means:
- clear/reset application database state
- clear/reset setup-complete flags
- clear/reset cached derived state
- clear/reset prior symbol validation state
- clear/reset stale signal/decision history
- clear/reset stale setup artifacts that would prevent first-run flow

After redeploy, the app must behave like a clean new install and show the Initial Setup flow again.

If there are persistent volumes or Railway DB persistence mechanisms that prevent this automatically, implement an explicit application-level reset/init path for deployment/startup.

I want deterministic clean-start behavior on each redeploy.

Important:
- if a full destructive reset on deploy is risky, document exactly what is being reset and why
- but the desired V1 behavior is: redeploy -> clean initialised app -> run setup again from scratch

---

# DO NOT CHANGE THESE

- Do NOT redesign the trading engine architecture
- Do NOT add new markets
- Do NOT change the 4-family architecture
- Do NOT lower the signal review visibility threshold from 75
- Do NOT turn this into a generic quant sandbox
- Do NOT keep obsolete strategy naming active in the UI or setup logic
- Do NOT leave old 29-instrument logic anywhere in setup/backfill/research

---

# V1 SYMBOL UNIVERSE (ONLY THIS LIST)

The entire system must use ONLY this exact V1 list:

- BOOM1000
- CRASH1000
- BOOM900
- CRASH900
- BOOM600
- CRASH600
- BOOM500
- CRASH500
- BOOM300
- CRASH300
- R_75
- R_100

The Initial Setup, backfill, backtester, scanner, diagnostics, research, and settings must use only these 12 symbols.

Remove all references to the old 29-symbol universe from:
- setup wizard
- backfill jobs
- research tab
- backtester
- defaults
- menu options
- validators
- diagnostics
- symbol pickers

---

# REQUIRED FIRST-RUN / INITIAL SETUP FLOW

After every redeploy/reset, the app must start in a clean “initial setup required” state.

The Initial Setup wizard must do exactly this, in this order:

## Step 1 — Input API Keys
Collect:
- Deriv Demo token
- Deriv Real token
- OpenAI API key

Required behavior:
- Save and verify tokens
- Save and verify OpenAI key
- If OpenAI key exists and ai_verification_enabled is unset, default AI Signal Verification to ON

This part is already mostly correct, but verify it is wired into the clean-start flow.

---

## Step 2 — Historical Backfill
Start a full historic tick-data backfill for each of the 12 V1 symbols.

Requirements:
- this step must NOT be skipped
- if a symbol backfill fails, retry or fail explicitly
- do not silently skip missing data
- track completion per symbol
- initial setup must not proceed as “complete” until backfill is truly done or explicitly failed and surfaced

I need a clear progress/status mechanism:
- symbol
- backfill started
- backfill progress
- backfill complete
- backfill failed
- retry status

This is a critical step.

---

## Step 3 — Strategy Backtest / Replay Simulation
Once historical data is fully backfilled for a symbol, the system must run a meaningful backtest/replay using the real engine logic we built.

This is the most important conceptual requirement.

### What I want
For each symbol, replay the available historical data as if the live engine had been running during that period.

Use the actual system logic:
- feature generation
- regime engine
- family routing
- scoring logic
- signal generation
- entry conditions
- exit logic
- TP / threshold / harvest logic as closely as possible

This should walk forward through history and simulate:
- when a signal would have qualified
- when a trade would have opened
- how long it would have been held
- whether TP / exit / harvest threshold would have closed it
- estimated outcome
- then continue scanning forward for the next valid trade

### The output per symbol must include:
- number of trades found
- strategy family hit count
- timestamps of hits
- estimated entry time/price
- estimated exit time/price
- average hold time
- total estimated profit/loss
- win rate
- score distribution
- trade count over full test period
- example timeline / sample trades

### Example
Backtest on BOOM1000:
- Trend Continuation: X hits
- Mean Reversion: Y hits
- Breakout / Expansion: Z hits
- Spike / Event: W hits
- Avg hold time: ...
- Estimated total return: ...
- Total trades: ...
- Score distribution: ...
- Example trades: ...

### Important
This is NOT a generic toy backtest.
It must use the real current trading logic.

If the current Research tab/backtester is not doing this, replace/refactor it so it does.

---

## Step 4 — External AI Review of Backtest
Once each symbol’s backtest completes, run the external AI model over the result.

The AI must comment on:
- which strategy family performs best on that instrument
- whether the instrument is suitable
- whether signal density is too low/high
- obvious warnings
- whether results look coherent or suspicious

The AI must review the actual backtest output, not generic commentary.

---

## Step 5 — Post-Backtest Optimization / Settings Initialization
Once the backtest has completed successfully, the system must initialize the trading settings based on the backtest results.

This means:
- AI must help set trading settings to suit the best-performing low-frequency, high-probability, long-hold behavior
- settings should be initialized for the most coherent V1 deployment mode
- focus on the style discussed throughout:
  - low frequency
  - high probability
  - long hold
  - maximize profits per trade
  - more selective, less noise
  - closer in spirit to the Forex Royals behavior we discussed

Do NOT randomly optimize for high-frequency churn.

---

## Step 6 — Start Live Runtime
Once setup, backfill, backtest, AI review, and settings initialization are all complete, then the app should navigate into the live system and:

- start Data Stream
- start scanner / signal engine
- start AI Signal Verification
- be ready for Paper / Demo / Real execution modes on top of the intelligence layer

This must be the final state after successful initial setup.

---

# SPECIFIC ISSUES THAT MUST ALSO BE ADDRESSED IN THIS PASS

These were previously identified and must be incorporated into the cleanup:

1. Initial Setup wizard was still scanning/backfilling the old 29 instruments
2. Stream Control did not auto-start correctly
3. After Initial Setup, Settings still showed “Initial Setup Required”
4. Dashboard still showed the capital banner/note/link that should be removed
5. Dashboard was unstable and kept reloading / showing loading screen every few seconds
6. Buttons overflowed sidebar/layout in desktop full-screen view
7. Tablet/phone/PWA support was unclear
8. AI Signal Verification did not reliably default on when OpenAI key existed
9. Research tab was outdated and conceptually wrong
10. Signals page must remain the decision review page
11. The app currently feels disconnected because old components/settings/functions linger after refactors

In this pass, these must be treated as part of one coherent cleanup.

---

# SIGNALS PAGE REQUIREMENT

Do NOT redesign Signals away from its current purpose.

Signals must remain the Decision Review page.

It must reflect the real decision engine and continue to show:
- meaningful visible decisions
- approved decisions
- blocked/rejected visible decisions
- correct regime/family/symbol/mode values

Keep visibility threshold = 75.

Do not lower it.

---

# CLEAN REFACTOR RULE

Before you add or change anything, check whether an old version of that logic already exists.

If it does:
- replace it properly
- migrate it properly
- remove dead code / dead menu items / dead settings
- do not leave parallel old/new logic behind

The final result must be coherent.

I want one app, one setup flow, one symbol universe, one backfill flow, one research model, one signals model.

Not layers of old and new behavior.

---

# RAILWAY REDEPLOY / RESET REQUIREMENT

On every code redeploy/rebuild in Railway:

Required end result:
- application starts clean
- database/app state is reset for first-run
- Initial Setup shows
- no stale prior setup data interferes
- no stale partial backfills interfere
- no stale decisions/signals interfere
- no stale setup-complete flags remain
- no stale model/backtest artifacts from previous app state remain unless explicitly intended and documented

If some data must persist for technical reasons, document exactly:
- what persists
- why
- how it is invalidated on new deployment

But the required operator experience is:
redeploy -> fresh clean initial setup

---

# OUTPUT REQUIRED

Return all of the following in detail.

## A. Root-Cause Summary
Explain why the app currently feels disconnected/incoherent and where old logic is lingering.

## B. Reset-on-Redeploy Plan
Explain exactly how you will make Railway redeploys reset to a clean first-run state.
Include:
- DB reset strategy
- setup state reset
- cache/artifact reset
- any migration/reset script used

## C. Initial Setup Flow
Show the exact new setup flow step by step.

## D. Backfill Design
Show:
- where historic backfill runs
- how progress is tracked
- how retries/failures are handled
- how setup blocks until complete

## E. Backtester / Replay Design
Show exactly how the new backtest/replay will work using the real engine logic.
Be explicit:
- where data comes from
- how the regime engine is used
- how families are routed
- how entries/exits are simulated
- what metrics are produced

## F. AI Review / Optimization Flow
Show how AI reviews each instrument’s backtest result and how settings are initialized from that.

## G. Files Changed
List every file changed.

## H. Dead / Removed Logic
List any obsolete menus, settings, functions, or flows removed in this pass.

## I. Remaining Deferred Items
List anything intentionally not fixed yet.

Do not avoid hard truths.
If something is still partial, say so directly.

Do not just patch around the issues.
Make the app coherent.

---

# ADDITIONAL NON-NEGOTIABLE REQUIREMENTS

## 1. NO TIME LIMIT ON INITIAL SETUP (CRITICAL)

There is NO time constraint on Initial Setup.

If Initial Setup takes:
- 30 minutes → acceptable
- 2 hours → acceptable
- several hours → acceptable

This is NOT a UX flow.
This is the FOUNDATION of the system.

Therefore:

- Do NOT shortcut backfill
- Do NOT partially complete symbols
- Do NOT skip failed symbols silently
- Do NOT mark setup complete until ALL required steps are complete

Setup must only complete when:
1. All symbols have successfully backfilled historical data
2. All symbols have completed full backtest/replay
3. AI review has been completed for each symbol
4. System settings have been initialized from those results

If anything fails:
- surface the failure clearly
- retry or block setup completion
- do NOT proceed in a degraded/partial state

This is a correctness-first system, not a speed-first system.

---

## 2. SIGNAL ENGINE MUST RUN INDEPENDENTLY OF TRADE MODES

Current conceptual issue:
The system is behaving like signals only matter when a trading mode is active.

This is WRONG.

### Required architecture

The system must be split cleanly into two layers:

### Layer 1 — Intelligence Layer (ALWAYS ON)
Runs whenever:
- Data Stream is active

This layer must:
- generate signals
- classify regime
- route to strategy families
- score signals
- apply thresholds
- run AI signal verification
- produce FINAL VERIFIED DECISIONS

This happens:
- EVEN IF no trading mode is active
- EVEN IF execution is disabled

### Layer 2 — Execution Layer (OPTIONAL)
Only runs when:
- Paper / Demo / Real mode is enabled

This layer:
- consumes already verified decisions
- executes trades based on them

---

---

## 3. AI VERIFICATION MUST ONLY RUN ON ELIGIBLE HIGH-QUALITY SIGNALS

AI verification must NOT run on every signal.

It must ONLY run on signals that meet the defined opportunity threshold.

### Required flow

For each signal:

1. Signal is generated
2. Composite score is calculated

3. Check threshold:
   IF compositeScore < visibilityThreshold (75):
      - Do NOT send to AI
      - Do NOT mark as opportunity
      - Optionally log internally for diagnostics only
      - STOP

   IF compositeScore ≥ visibilityThreshold (75):
      - This is now an ELIGIBLE OPPORTUNITY
      - Send to AI verification

4. AI returns:
   - agree
   - disagree
   - uncertain

5. System logs the result:
   - symbol
   - family
   - regime
   - composite score
   - EV
   - AI verdict

6. Signal is marked as:
   "TRADE OPPORTUNITY"

---

### Important constraints

- AI must NEVER be called for low-quality signals below threshold
- AI must ONLY operate on filtered, high-confidence candidates
- This ensures:
  - cost control
  - signal quality integrity
  - no noise in decision logs

---

## 4. DECISION CLASSIFICATION (UPDATED)

Only signals that pass threshold AND go through AI should appear in the Signals / Decision Review page.

Each row must represent:
"A high-quality, threshold-qualified, AI-reviewed opportunity"

Statuses must reflect:
- approved (AI agree)
- rejected (AI disagree)
- uncertain (AI uncertain)
- blocked (execution-layer reason, if applicable)

Low-score signals must NOT appear in the Signals page.
They may be logged separately for diagnostics if needed.

---
5. System logs:
   - signal
   - score
   - regime
   - family
   - AI verdict

6. Signal is marked as:
   "TRADE OPPORTUNITY" (even if not executed)

This must happen regardless of:
- trading mode being OFF
- execution being disabled

---

## 4. TRADING MODES MUST FOLLOW VERIFIED SIGNALS — NOT GENERATE THEM

When a trading mode is active:

It must NOT:
- re-evaluate raw signals independently
- bypass AI verification
- run its own separate logic

It must ONLY:
- consume signals that have already:
  - passed threshold
  - been AI verified

Execution flow must be:

signal → scoring → AI verification → decision logged → (optional) execution

NOT:

signal → execution → AI later

---

## 5. DECISION LOG MUST REPRESENT TRUE OPPORTUNITIES

The Decision Review / Signals page must show:

- all meaningful trade opportunities
- not just executed trades
- not just mode-enabled trades

Each row must represent:

"A valid, AI-reviewed opportunity the system identified"

Include:
- symbol
- family
- regime
- score
- EV
- AI verdict
- status:
   - approved (AI agree)
   - rejected (AI disagree)
   - uncertain
   - blocked (execution layer reason, if applicable)

Execution status must be a SECONDARY attribute, not the definition of whether something is logged.

---

## 6. DO NOT TIE SIGNAL GENERATION TO MODE FLAGS

Explicit requirement:

Remove any logic where:
- scanner only runs if a mode is active
- decisions are only stored if a mode is active
- AI verification only runs if a mode is active

Scanner + AI + decision logging must be independent of execution modes.

---

## 7. FINAL SYSTEM BEHAVIOR (TARGET STATE)

After setup:

If:
- Data Stream = ON
- No trading modes active

Then:
- signals are generated
- signals are scored
- AI verifies signals
- decisions are logged
- Signals page populates
- system runs in "intelligence-only" mode

If:
- Data Stream = ON
- Trading mode active

Then:
- SAME signals are used
- SAME AI decisions are used
- execution layer acts on those signals

No duplication of logic.
No parallel pipelines.

---

## 8. OUTPUT REQUIREMENT (ADDITIONAL)

In your response, also include:

### J. Signal vs Execution Architecture
Show:
- exact separation between intelligence layer and execution layer
- files/functions where this separation is enforced

### K. Decision Lifecycle
Show full lifecycle:
data → features → regime → family → score → AI → decision log → execution (optional)

### L. Verification Example
Provide one example:

- symbol
- score ≥ 75
- AI verdict
- decision logged
- execution OFF → logged only
- execution ON → same decision used for trade

---

Do not ignore this section.
This is critical to aligning the system with how it is intended to operate.
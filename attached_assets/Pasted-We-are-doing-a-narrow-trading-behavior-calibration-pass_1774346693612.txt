We are doing a narrow trading-behavior calibration pass for V1.

Do NOT redesign the engine.
Do NOT change the 4-family architecture.
Do NOT widen the symbol universe.
Do NOT touch the setup/backfill/research work in this pass.
Do NOT lower the signal visibility threshold.
Do NOT add new features unless required to fix the current trade behavior.

Focus only on this problem:

The system is generating trades that do not make sense operationally:
- capital allocation is too small
- trades are closing too quickly
- SL and TP are too close to entry
- behavior does not match the intended model:
  low-frequency, high-probability, longer-hold, larger-profit-per-trade

The last three prompts about setup/reset/research still stand, but do not work on them in this pass.
This pass is only about fixing the live trade behavior.

---

# TARGET BEHAVIOR

The system must aim for:

- fewer trades
- higher conviction
- larger capital deployment on strong setups
- wider SL/TP based on actual structure/volatility
- longer average hold times
- less noise trading
- less tiny profit scalping
- more realistic “swing-style” or “position-style” behavior

The engine should not be opening trades that look like:
- tiny size
- entry and exit a few points apart
- hold time of minutes unless explicitly justified
- low-value churn

---

# CURRENT PROBLEM TO DIAGNOSE

I need you to inspect exactly why current paper trades are behaving like this.

Specifically investigate:

1. Why is capital allocation so small?
2. Why are SL and TP so close to entry?
3. Why are trades closing so quickly?
4. Is the system effectively scalping despite the intended architecture?
5. Are the family settings, allocation tiers, ATR/volatility logic, TP multipliers, SL ratios, harvest logic, or time exits causing this?

Do not guess.
Trace the code and prove the reasons.

---

# REQUIRED DIAGNOSTIC OUTPUT

Return a root-cause analysis for these four areas:

## A. Capital Allocation
Explain exactly how position size is currently determined.

I need:
- files/functions involved
- how score affects allocation
- how entry stage affects size
- how mode settings affect size
- whether family-specific modifiers exist
- whether current caps are making trades too small

Show one real example using a recent trade:
- symbol
- score
- allocation %
- entry stage
- final size
- why that size was produced

---

## B. Stop Loss / Take Profit Placement
Explain exactly how SL and TP are currently calculated.

I need:
- files/functions involved
- whether ATR-based logic is being used
- whether SL/TP are based on volatility, structure, or just multipliers
- whether current ratios are too tight for Boom/Crash behavior
- whether TP multipliers and SL ratios are still set too low

Show one real example from recent trades:
- symbol
- entry
- SL
- TP
- ATR / volatility inputs
- exact formulas used

---

## C. Exit Timing / Short Holds
Explain exactly why trades are being closed quickly.

Investigate:
- fixed TP hit too fast
- harvest logic firing too early
- trailing stop too tight
- time-exit too short
- regime flip forcing close
- AI override / execution filter closing early
- any hidden close logic

Show:
- file/function
- exact close condition
- one recent example with reason

---

## D. Family Behavior
Determine whether the 4 families are actually behaving like:
- Trend Continuation
- Mean Reversion
- Breakout / Expansion
- Spike / Event

Or whether they are still effectively acting like short-hold signal bots.

Show average behavior by family from recent trades:
- trade count
- average hold time
- average SL distance
- average TP distance
- average allocation
- average realized PnL

If there is not enough recent data, infer from code paths and current config.

---

# REQUIRED FIXES

After diagnosis, implement the minimum necessary changes to align live behavior with the intended model.

## 1. Increase Selectivity for Execution
Do not reduce signal visibility.
But execution should prefer stronger setups.

Review and adjust:
- execution threshold logic
- family-specific minimum score
- any allocation gating

The goal is fewer but stronger live trades.

---

## 2. Increase Practical Capital Deployment
Fix the sizing logic so that when a real high-quality setup is approved, the deployment is meaningful.

Review and adjust:
- allocation tiers
- entry-stage multipliers
- family-specific allocation modifiers
- overly conservative caps

Do NOT make it reckless.
Do make it materially larger than the current tiny allocations if the setup is strong.

---

## 3. Widen SL / TP to Match Intended Hold Style
SL and TP must not be tiny unless volatility genuinely justifies it.

Review and adjust:
- ATR multipliers
- SL ratio
- TP multipliers
- family-specific exit profiles
- instrument-specific volatility scaling

The target is:
- wider SL
- wider TP
- more room for the thesis to play out
- longer average holds

---

## 4. Prevent Premature Harvesting / Early Close
If harvest, trailing, or time-exit logic is causing early closure, fix it.

Review and adjust:
- peak drawdown harvest threshold
- min peak required before harvest
- trailing stop aggressiveness
- time-exit windows
- family-specific hold expectations

The system should not kill good trades too early.

---

## 5. Add Family-Specific Hold Profile
Each family should have a hold-style profile.

At minimum implement or verify:

### Trend Continuation
- medium to long hold
- wider target
- allow trend to develop

### Mean Reversion
- medium hold
- not immediate scalp unless code explicitly intends it

### Breakout / Expansion
- medium to long hold
- must allow expansion move to breathe

### Spike / Event
- can be opportunistic, but still not absurdly tight unless volatility justifies it

If no family-specific hold profile exists yet, add one.

---

# HARD CONSTRAINTS

Do NOT:
- break the existing decision review page
- touch the initial setup/backtester flow in this pass
- widen scope into research redesign
- add cosmetic UI changes unless needed for debugging
- convert the engine into an HFT or scalping system

Do:
- fix real live trade behavior
- make the system act more like the intended selective longer-hold engine

---

# REQUIRED OUTPUT

Return exactly this:

## A. Root Cause Analysis
1. Why capital allocation is too small
2. Why SL/TP are too tight
3. Why holds are too short
4. Which families are affected most

## B. Files Changed
For each changed file:
- file name
- function(s)
- what was wrong
- what was changed

## C. Before vs After
Show one or two recent trade examples and how the new logic would size/manage them differently.

## D. New Behavioral Defaults
List the final effective settings for:
- allocation tiers
- entry stage multipliers
- SL ratios
- TP multipliers
- trailing / harvest thresholds
- time-exit windows
- any family-specific hold settings

## E. What To Verify Next
Give 5 specific things I should check after redeploy:
- on Trades page
- on Signals page
- in logs
- in hold times
- in sizing

Do not give a vague answer.
Do not just explain.
Diagnose and fix the actual live trade behavior.
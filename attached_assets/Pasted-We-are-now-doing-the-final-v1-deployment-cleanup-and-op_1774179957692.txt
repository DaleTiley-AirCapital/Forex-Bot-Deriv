We are now doing the final v1 deployment cleanup and operator visibility pass.

Do not expand scope.
Do not add new features beyond what is specified.
Do not redesign the engine.

This is about:
- fixing UI inconsistencies
- making existing features work correctly
- exposing decision data clearly for operator review
- preventing the Signals menu from becoming spam

---

# 1. REMOVE UNUSED UI VIEWS (STRICT)

## Dashboard & Trades menus

REMOVE the “All” view completely.

Do NOT keep it.
Do NOT relabel it.
Do NOT aggregate anything.

Only keep:
- Paper
- Demo
- Real

Ensure:
- no backend logic depends on “All”
- no hidden aggregation still runs
- no UI references remain

---

## Risk Menu

REMOVE “Aggregated Risk” completely.

Do NOT rename it.
Do NOT keep a global version.

Only show:
- Paper Risk
- Demo Risk
- Real Risk

Ensure:
- no calculations depend on aggregated risk
- no UI elements reference it
- no backend code still computes it unnecessarily

---

# 2. FIX EXISTING "SIGNALS" MENU (CRITICAL)

There is already a Signals menu item.

We do NOT want a new page.
We must FIX and REPURPOSE this existing Signals menu.

---

## REQUIRED BEHAVIOR OF SIGNALS MENU

The Signals menu must now become the **Decision Review Table**.

It must show:
- approved signals
- executed trades
- meaningful rejected signals (NOT spam)

---

## SIGNAL VISIBILITY RULES (MANDATORY)

The system scans frequently, so DO NOT show every rejected signal.

Only store/show signals that meet:

### ALWAYS SHOW
1. Approved signals
2. Executed trades
3. Signals where AI changed the outcome

### CONDITIONALLY SHOW (QUALITY FILTER)
4. Rejected or blocked signals ONLY IF:
   - composite score >= visibility threshold

### DO NOT SHOW
- low-score rejected signals
- noisy candidates
- spam from frequent scans

---

## VISIBILITY THRESHOLD

Add a global setting:

- Signal Review Visibility Threshold
- default = 75

Rules:
- if score < 75 AND rejected → DO NOT store/show
- if score >= 75 → show
- approved signals always show

IMPORTANT:
This threshold is ONLY for UI visibility.
It must NOT affect trading logic.

---

## REQUIRED COLUMNS

For each visible signal:

- timestamp
- mode (paper / demo / real)
- symbol
- regime
- regime confidence
- allowed strategy families
- selected strategy family
- sub-strategy (if applicable)
- composite score
- expected value (EV)
- reward/risk ratio
- AI verdict
- router decision:
  - approved
  - rejected
  - blocked
  - skipped
- rejection reason
- allocation % (if approved)
- execution status

---

## REQUIRED FILTERS

- date range
- symbol
- strategy family
- mode
- approved only
- rejected only
- AI approved / AI rejected

---

## DATA SOURCE

Signals must come from:
- signalRouter decisions
- scheduler outputs
- AI verification layer

If needed:
- create signalDecisionsTable

DO NOT persist all low-quality signals.
Persist only based on visibility rules.

---

## REQUIRED OUTCOME

After a few days of running, I must be able to open Signals and answer:

- what meaningful setups the system saw
- what good setups were rejected
- why they were rejected
- what executed
- whether regime selection makes sense
- whether AI is helping or hurting

---

# 3. REGIME VISIBILITY

Each Signals row must include:

- regime label
- regime confidence
- allowed strategy families

This is mandatory for debugging.

---

# 4. STRUCTURED LOG ALIGNMENT

Ensure:
- Signals table aligns with structured logs
- one row = one meaningful evaluated decision

Do NOT log spam into Signals.

---

# 5. REMOVE UI / ENGINE MISMATCHES

Clean up:

- remove macro-bias completely
- ensure ONLY 4 strategy families exist everywhere
- remove any old 6-strategy references
- remove any non-v1 symbols
- ensure consistent naming across UI and backend

---

# 6. VERIFY SIGNAL PIPELINE

Show proof:

data → regime → allowed family → scoring → routing → AI → decision → Signals table

Also show where visibility filtering is applied.

---

# 7. SETTINGS

Add:

- Signal Review Visibility Threshold (default = 75)

This must:
- control ONLY visibility/storage
- NOT affect execution

---

# 8. OUTPUT REQUIRED

Return:

1. files changed
2. confirmation “All” view fully removed
3. confirmation “Aggregated Risk” fully removed
4. how Signals table is populated
5. where visibility threshold is applied
6. table schema
7. example approved signal
8. example rejected (visible) signal
9. confirmation low-score spam is excluded
10. confirmation regime + AI fields are included

---

# FINAL GOAL

The Signals menu must become:

the single source of truth for:
- meaningful opportunities
- why strong setups were rejected
- what the system executed

No new pages.
No spam.
Clean UI.
Accurate data.
We are now fixing the final runtime and UI-label issues for v1.

Do not redesign the engine.
Do not add new features outside this scope.

## 1. FIX THE REAL PROBLEM: STREAM + SCANNER NOT RUNNING

The Overview screenshot shows:
- Data Stream = OFFLINE
- Signal Scanner = IDLE

The Signals page is empty because the runtime is not actually active.

I need you to investigate and fix the startup/runtime path so that on boot:

1. validated v1 symbols are subscribed
2. data stream status becomes RUNNING/LIVE
3. scanner starts automatically
4. scanner status becomes ACTIVE/RUNNING
5. decision records begin being written when eligible signals occur

Return exact code-level proof of:
- where stream startup is triggered
- where scanner startup is triggered
- why they are currently ending up OFFLINE / IDLE
- what was fixed

## 2. ENSURE SIGNAL DECISIONS ARE WRITTEN

Verify the decision logging pipeline.

I need proof of:
- where a candidate signal becomes a decision record
- where that record is inserted into the DB
- whether rejected visible signals are also stored
- whether approved signals are stored
- whether the visibility threshold is accidentally blocking all inserts

If the current logic only writes after execution, fix it.
Signals/decisions must be stored at decision time, not only after trade execution.

## 3. UI LABEL CLEANUP FOR STRATEGY FAMILIES

Keep internal keys as:
- trend_continuation
- mean_reversion
- breakout_expansion
- spike_event

But in the UI, display them as:
- Trend Continuation
- Mean Reversion
- Breakout / Expansion
- Spike / Event

Apply this to:
- Signals filters
- badges
- settings
- diagnostics
- anywhere else visible to operator

Do not expose snake_case family names in UI.

## 4. SIGNALS PAGE EMPTY-STATE IMPROVEMENT

If there are no decisions yet, the empty state must explain why.

Show a useful status message such as:
- No decisions logged yet.
- Data Stream: Offline/Running
- Scanner: Idle/Running
- Last scan time
- Last symbol scanned
- Visibility threshold currently set to X

This must help the operator understand whether:
- the system is idle correctly
- or the pipeline is broken

## 5. OVERVIEW STATUS WIRING

The Overview page operational status must reflect the real runtime state.

Verify and fix:
- Data Stream status source
- Signal Scanner status source
- Deriv API status source

These must be based on actual runtime state, not stale defaults.

## 6. OUTPUT REQUIRED

Return:
1. exact reason Data Stream was OFFLINE
2. exact reason Signal Scanner was IDLE
3. exact files changed
4. exact function(s) fixed
5. proof that decision records are now being written
6. one sample decision row
7. confirmation that UI family labels are human-readable
8. screenshot guidance for where I should verify the fixes
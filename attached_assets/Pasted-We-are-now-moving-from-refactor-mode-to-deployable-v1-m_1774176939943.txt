We are now moving from refactor mode to deployable v1 mode.

Do not redesign the architecture further.
Do not add more features unless explicitly requested.

Implement a deployment-hardening pass for Railway with the goal of getting a stable v1 live for signal accumulation, validation, and controlled paper/demo execution.

## DEPLOYMENT OBJECTIVE
Ship a constrained but correct v1 that:
- starts automatically
- streams automatically
- validates symbols on boot
- uses only a clean deployable symbol set
- runs the 4-family engine
- logs every decision clearly
- supports Paper and Demo cleanly
- is Railway-ready

## 1. FREEZE THE CORE ENGINE
Do not refactor the 4-family orchestration again.
Keep:
- regime-first routing
- 4 strategy families
- model routing
- conflict resolution
- harvesting
- extraction engine
- AI-aware validation

## 2. DEPLOYABLE SYMBOL SET ONLY
For v1, enable only these symbols:

Boom/Crash:
- BOOM1000
- CRASH1000
- BOOM500
- CRASH500
- BOOM300
- CRASH300
- BOOM600
- CRASH600
- BOOM900
- CRASH900

Volatility:
- R_75
- R_100

Disable from deployment for now:
- Step family
- Range Break family
- Bull/Bear family
- Jump family
unless active_symbols proves exact mappings and live streams are correct.

## 3. AUTO-START BEHAVIOUR
On server boot:
- validate all configured symbols
- start streams automatically for valid symbols
- mark invalid symbols disabled
- start scheduler automatically unless explicitly disabled
- run stale-stream watchdog automatically

## 4. AI DEFAULT
If OpenAI API key exists and is non-empty:
- ai_verification_enabled should default to true
If no key exists:
- ai_verification_enabled defaults to false
User can still override in UI.

## 5. UI CLEANUP FOR V1
Implement only critical cleanup:
- remove “All” tab
- remove “Aggregated Risk”
- remove “Deriv API (Legacy)”
- strategies UI must show 4 families only
- instruments UI must show only the deployable validated list
- diagnostics tab must clearly show live/stale/invalid per symbol

## 6. RAILWAY READINESS
Add or verify:
- production startup command
- health endpoint
- environment variable docs
- persistent DB config expectations
- safe default mode on boot
- clean logging
- no blocking debug-only code

## 7. LOGGING / OBSERVABILITY
For every scan/evaluation cycle, log:
- symbol
- regime
- allowed family/families
- chosen family
- score
- EV
- rejection reason if rejected
- AI verdict if AI enabled
- allocation %
- execution result
- harvest close reason
- extraction trigger reason

## 8. OUTPUT REQUIRED
Return:
1. files changed
2. exact deployable symbol set in code
3. startup sequence
4. Railway deployment steps
5. remaining non-blocking issues to ignore for v1

Do not expand scope. Harden and prepare for deployment.
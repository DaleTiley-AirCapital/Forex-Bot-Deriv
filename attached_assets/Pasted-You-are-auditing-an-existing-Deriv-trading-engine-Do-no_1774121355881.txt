You are auditing an existing Deriv trading engine. Do not rewrite the system blindly.

Your job is to:
1. Inspect the current codebase
2. Explain exactly what exists
3. Identify gaps vs the intended architecture
4. Show how the system currently behaves vs how it should behave
5. Propose precise changes BEFORE implementing anything

Be brutally honest.

---

# SYSTEM OBJECTIVE

This is NOT a high-frequency trading bot.

This is a:
LOW-FREQUENCY, HIGH-PROBABILITY, LONG-HOLD, CAPITAL-DEPLOYMENT ENGINE

The intended behaviour:

- always scanning
- rarely trading
- only taking top-tier setups
- holding trades for hours or days
- allowing multiple entries into a single idea
- allowing multiple strategies on the same asset
- harvesting profit before full TP
- stopping trading when no elite setups exist
- operating in capital extraction cycles (not infinite compounding)

---

# REQUIRED STRATEGIES

1. Boom Hunter
- spike probability / drift-then-spike positioning
- long hold
- Boom assets

2. FXR
- mean reversion / exhaustion / reversal
- medium-long hold
- Boom + Crash

3. SkyBreak
- compression → breakout → expansion
- long hold
- Boom + Crash

4. COWS
- regime filter / cycle engine / trade permission layer
- may not place trades directly

---

# REQUIRED SYSTEM LAYERS

Audit the following:

A. Data Layer
- historical tick collector
- live data stream
- candle aggregation
- spike detection
- feature generation

B. Research Layer
- backtesting engine
- walk-forward testing
- parameter optimisation
- performance tracking

C. Strategy Layer
- separate modules for Boom Hunter, FXR, SkyBreak, COWS
- entry rules
- exit rules
- asset restrictions
- long-hold logic
- position scaling capability

D. Decision Layer
- regime filter (REAL, not indicator hack)
- score threshold
- confidence scoring
- expected value filtering
- strategy routing (CRITICAL)

E. Portfolio Layer
- capital allocation engine
- multi-asset ranking
- conflict resolution when multiple signals appear
- allocation based on signal strength

F. Execution Layer
- trade placement
- scaling into positions
- SL/TP handling
- long-hold support
- trade lifecycle tracking

G. Risk Layer
- per-trade risk
- max exposure
- drawdown control
- capital extraction logic
- reset logic

---

# 🔴 NEW SECTION — MODEL ENGINE (CRITICAL)

Explain EXACTLY:

## 1. Model Per Instrument
- Does each instrument (Boom 300, Crash 500, etc.) have its own model?
- Or is one shared model used?
- How are models separated or reused?

## 2. What the Model Analyses
List ALL features being used:

- price-based features (candles, ticks)
- volatility metrics
- momentum indicators
- structure / trend features
- spike timing / tick distance
- regime classification inputs
- derived statistical features

## 3. Model Type
- What models are used? (ML / rules / hybrid)
- logistic regression / tree models / neural / rule engine?
- Is OpenAI actually used for prediction or just validation?

## 4. Training Process
Explain clearly:

- how training data is generated
- how labels are defined (what is a “win”?)
- how spike events are labelled
- how training windows are selected
- whether walk-forward is actually implemented
- how often retraining occurs
- how overfitting is prevented

## 5. Output of Model
What does the model produce:

- probability score?
- direction?
- expected value?
- classification?
- confidence?

---

# 🔴 NEW SECTION — AI / OPENAI INTEGRATION

Explain EXACTLY:

- what the OpenAI API is doing
- is it:
  - generating signals?
  - validating signals?
  - optimizing parameters?
  - acting as a decision engine?

- how prompts are structured
- what inputs are sent to AI
- what outputs are expected
- whether AI is overriding deterministic logic
- whether AI is used per trade or per batch

CRITICAL:
Does the AI understand which strategy it is working for?
Or is it acting as a generic “approve/reject everything” layer?

---

# 🔴 NEW SECTION — STRATEGY EXECUTION ANALYSIS

For EACH strategy:

- file/module name
- entry logic
- exit logic
- asset scope
- hold time behaviour
- position scaling behaviour
- whether it is actually low-frequency
- whether it matches intended behaviour

Answer:

Is it:
- scalping
- intraday
- swing
- long-hold

---

# 🔴 SIGNAL ROUTING (MOST IMPORTANT)

Explain:

- how signals are currently generated
- how the system decides WHICH strategy owns a signal
- whether signals are broadcast to all strategies (BAD)
- whether a proper router exists

REQUIRED TARGET:

signal → regime classification → strategy mapping → scoring → ranking → execution

---

# 🔴 MULTI-ASSET + CAPITAL ALLOCATION

Explain:

- how multiple signals are handled
- are they ranked?
- how capital is split
- whether strongest setups get more capital
- whether weak setups are blocked

---

# 🔴 EXIT LOGIC

Explain:

- is exit only TP/SL?
- is there profit harvesting?
- is peak profit tracked?
- is drawdown from peak used?
- are trades closed before TP?

---

# 🔴 POSITION BUILDING

Explain:

- can the system scale into positions?
- can multiple entries be added?
- are positions capped?

---

# 🔴 EXTRACTION ENGINE

Explain:

- is capital cycling implemented?
- can system:
  - hit target
  - withdraw profit
  - reset capital
- or is it just compounding?

---

# REQUIRED OUTPUT FORMAT

### 1. Current architecture summary

### 2. Strategy mapping

### 3. Model engine explanation

### 4. AI integration explanation

### 5. Signal routing explanation

### 6. Portfolio + capital allocation explanation

### 7. Exit logic explanation

### 8. Position building capability

### 9. Extraction model status

### 10. Gaps vs required system

---

# TABLE FORMAT (MANDATORY)

Component | Exists? | File/Module | Current Behaviour | Missing Behaviour | Required Fix

---

# FINAL STEP

DO NOT CODE YET.

After full audit:
- provide exact implementation plan
- list files to change
- list modules to add
- list logic to rewrite

Then ask for approval.
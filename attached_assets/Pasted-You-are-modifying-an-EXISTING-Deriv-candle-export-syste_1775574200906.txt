You are modifying an EXISTING Deriv candle export system.

Goal:
Rebuild ONLY the export layer so that the current Excel and JSON outputs are replaced or supplemented with exports that are fully suitable for detailed downstream ChatGPT analysis.

Critical constraints:

Do NOT modify trading logic.
Do NOT modify signal logic.
Do NOT modify entry/exit logic.
Do NOT modify backfill logic except where strictly required to improve export generation.
Do NOT modify database structure unless absolutely necessary for export-only purposes.
Do NOT optimize or refactor unrelated code.
Do NOT touch production behavior outside the export pipeline.
This task is export-format redesign only.
Preserve all raw candle values exactly as stored/fetched.
We are still in research mode, not production mode.

Objective:
The existing exports are causing runtime/context issues for AI analysis because they are too large, too monolithic, and not structured for reliable ingestion. Rebuild the export process so the exact same raw data can be exported in deterministic, integrity-checked, AI-friendly chunks.

Required output design:

A. Export formats
For every candle export, generate:

chunked JSON files as the primary research format
optional CSV files as secondary format
optional Excel only for human review, never as the primary AI analysis format

B. JSON structure
Use plain raw candle JSON, not summaries and not strategy output.
Each chunk file must contain:

symbol
timeframe
exportDate
chunkIndex
totalChunks
candleCount
firstTimestamp
lastTimestamp
candles: [ ... raw candles ... ]

Each candle must preserve exactly:

timestamp
open
high
low
close
volume

Do not add strategy labels.
Do not classify trades.
Do not add AI commentary.
Do not remove any candles.
Do not alter candle order.

C. Chunking rules
Implement deterministic chunking for 1m candles.

Default rule:

split exports into monthly files first

If a monthly file exceeds the safe AI-analysis size threshold, split it further into fixed-size sub-chunks.

Target chunk size:

preferred: 20,000 to 30,000 candles per file
maximum: 50,000 candles per file

Examples:

BOOM300_2025-09_chunk_001.json
BOOM300_2025-09_chunk_002.json
CRASH300_2025-10_chunk_001.json

If monthly candle count is already within safe range, only create one monthly chunk.

D. Manifest file
For each export set, generate a manifest file:

symbol
timeframe
exportCreatedAt
totalCandles
sourceStartTimestamp
sourceEndTimestamp
totalChunks
chunkFiles: [
{
fileName,
candleCount,
firstTimestamp,
lastTimestamp
}
]

Example file:

BOOM300_manifest.json

E. Validation file
For each export set, generate a validation file:

symbol
timeframe
totalCandlesExpected
totalCandlesExported
duplicateTimestampCount
missingIntervalCount
timestampsStrictlyAscending
firstTimestamp
lastTimestamp
validationPassed
notes

Validation rules:

Check timestamps are sorted oldest to newest
Check for duplicate timestamps
Check for missing 1-minute intervals
Check exported candle count matches source query count
Do not silently ignore problems
If validation fails, record it clearly

Important:
Missing intervals must only be reported, not filled.
Duplicate timestamps must only be reported, not silently removed unless current code already has an explicit dedupe rule. If dedupe already exists, do not change it yet without flagging it.

F. Export folder structure
Create a clean deterministic structure like:

exports/
BOOM300/
1m/
manifest.json
validation.json
BOOM300_2025-09_chunk_001.json
BOOM300_2025-09_chunk_002.json
BOOM300_2025-10_chunk_001.json

Same structure for each symbol/timeframe.

G. Performance requirements
Make the export logic safe for large backfills:

do not build giant in-memory blobs if avoidable
stream/process in batches where possible
write chunk by chunk
avoid pretty-printed oversized JSON unless configurable
keep export generation deterministic and repeatable

H. Backward compatibility
Do not break the current system.
Implement this as an export-layer improvement only.

If existing Excel/JSON exports are currently relied on elsewhere:

keep legacy export option available behind a flag
add the new research-friendly export mode alongside it

Example flags:

export_mode="legacy"
export_mode="research"

Default should remain whichever is currently safest for the existing app unless explicitly changed by me.

I. Deliverables required from you
Before finalizing, show me:

which files you changed
exactly what export functions were changed or added
the new folder/file naming convention
one sample manifest structure
one sample validation structure
one sample chunk JSON structure
confirmation that no trading or signal logic was changed

J. Explicit non-goals
Do NOT:

redesign the strategy
optimize the bot
change the database model broadly
change API behavior unrelated to exports
change execution flow of the trading engine
create summaries of the market
classify setups
create AI research conclusions

K. Success criteria
This task is successful only if:

raw candle exports remain exact
exports are chunked into ChatGPT-safe analysis sizes
validation metadata is included
files are deterministic and easy to upload
no unrelated app logic was changed

Now inspect the existing export code and implement the minimum necessary changes to produce this new research-friendly export mode safely.